const init = {

}

const homeReducer = (state = init, action) => {
    switch (action.type) {

        default:
            {
                return state;
            }
    }
};

export default homeReducer;